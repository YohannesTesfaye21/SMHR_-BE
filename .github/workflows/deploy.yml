name: Deploy

on:
  workflow_run:
    workflows: ["CI - Build and Test"]
    types:
      - completed
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "."
          target: "/opt/smhfr-be"
          overwrite: true
          rm: false
          strip_components: 0
          exclude: |
            .git
            .github
            **/bin
            **/obj
            **/.vs
            **/.vscode
            *.md

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /opt/smhfr-be
            export COMPOSE_PROJECT_NAME=smhfr-be
            
            # Create .env file
            {
              echo "DB_HOST=postgres"
              echo "DB_PORT=5432"
              echo "DB_NAME=smhfr_db"
              echo "DB_USER=postgres"
              echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}"
            } > .env
            
            # Start Postgres (preserve data)
            docker compose --env-file .env up -d postgres

            # Wait for Postgres to be ready
            echo "⏳ Waiting for Postgres to be ready..."
            timeout 60 bash -c 'until docker compose exec -T postgres pg_isready -U postgres > /dev/null 2>&1; do sleep 1; done'
            echo "✅ Postgres is ready"

            # Set password directly (Unix socket connection, no password needed)
            echo "🔐 Setting PostgreSQL password..."
            if ! docker compose exec -T postgres psql -U postgres -d postgres -c "ALTER USER postgres WITH PASSWORD '${{ secrets.DB_PASSWORD }}';"; then
              echo "❌ ERROR: Failed to set PostgreSQL password"
              docker compose logs postgres --tail=20
              exit 1
            fi
            echo "✅ Password set successfully"
            
            # Give PostgreSQL a moment to process the password change
            sleep 2

            # Verify password works
            echo "🔍 Verifying password..."
            if ! PGPASSWORD='${{ secrets.DB_PASSWORD }}' docker compose exec -T -e PGPASSWORD='${{ secrets.DB_PASSWORD }}' postgres psql -h localhost -U postgres -d postgres -c "SELECT 1;" > /dev/null 2>&1; then
              echo "❌ ERROR: Password verification failed"
              echo "📋 PostgreSQL logs:"
              docker compose logs postgres --tail=30
              exit 1
            fi
            echo "✅ Password verified"
            
            # Stop API if running to clear connection pool
            docker compose stop api 2>/dev/null || true
            sleep 2
            
            # Build and start API (keep postgres running)
            docker compose --env-file .env up -d --build --no-deps api
            
            # Wait and check health
            echo "⏳ Waiting for API to be healthy..."
            for i in {1..30}; do
              if curl -f http://localhost:8080/api/health > /dev/null 2>&1; then
                echo "✅ Deployed successfully!"
                exit 0
              fi
              sleep 2
            done
            echo "❌ Health check failed"
            docker compose logs api --tail=50
